# Brand assets

- `snowglobe-logo.png` is the full-resolution generated source image.
- `snowglobe-logo.webp` is the optimized web and README asset.

The logo intentionally uses a generic crystalline snowflake rather than the Snowflake corporate mark. It contains no text so the same asset can be reused in the application, documentation, and repository artwork.
